import '@emotion/react'

declare module '@emotion/react' {
  export interface Theme {
    colors: {
      primary: string
      secondary: string
      primary: string
      secondary: string
      base: string
      baseLight: string
      baseDark: string
      coolMint: string
      ltGray: string
      gray: string
    }
  }
}
